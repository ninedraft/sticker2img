# sticker2img

Bot, which converts Telegram stickers to jpeg and png images

## Installation
```shell
go get -u github.com/ninedraft/sticker2img
```

## Setup
```shell
sticker2img -token "TOKEN" -debug SET_TRUE_IF_WANT_DETAILED_LOG
```
